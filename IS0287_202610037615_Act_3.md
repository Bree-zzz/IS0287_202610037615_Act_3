# Reporte de Evaluación: IS0287_202610037615_Act_3

## 1. Revisión de Estructura y Archivos Base
- **.gitignore**: Presente y con configuración correcta para un proyecto de Java.
- **README.md**: Presente, pero sin contenido descriptivo (solo contiene el texto "m").
- **Compilación y Ejecución**: La aplicación **NO COMPILA** debido a un error de sintaxis en `src/CreditoCorporativo.java` (falta un punto y coma al final de la línea 57).

## 2. Evaluación por Criterios (Base 50 puntos)

### Encapsulamiento (30% = 15 puntos posibles)
- **Criterio de Éxito:** Los atributos son private. No es posible asignar saldos negativos ni acceder a variables internas directamente.
- **Análisis:** Los atributos están declarados como `private`. Se implementaron las validaciones para evitar saldos negativos en los métodos mutadores. En la clase `GestorDeCobranza`, el estudiante comentó la línea `//c.SaldoPendiente=0;` para demostrar el error de compilación por el acceso directo. Sin embargo, se penaliza porque la validación implementada en el setter tiene el error de sintaxis que rompe todo el programa.
- **Puntos Asignados:** 10 / 15 puntos.

### Sobrecarga (20% = 10 puntos posibles)
- **Criterio de Éxito:** Los 3 constructores funcionan correctamente e inicializan el objeto en estados válidos.
- **Análisis:** Se declararon 3 constructores. No obstante, el **Constructor 1** (Estándar) está incorrecto porque omite inicializar el atributo `SaldoPendiente` (dejándolo en 0 por defecto), a pesar de que el enunciado exigía: *"El saldo inicial es igual al monto."*. A raíz de este error, en la clase `Main` el estudiante utilizó el Constructor 2 en lugar del Constructor 1 exigido para la prueba.
- **Puntos Asignados:** 5 / 10 puntos.

### Lógica de Negocio (25% = 12.5 puntos posibles)
- **Criterio de Éxito:** El cálculo de esRiesgoso es dinámico y correcto. La validación de abonos funciona.
- **Análisis:** Se implementó correctamente el método `ActualizarRiesgo()`, el cual evalúa si el saldo supera el 120% del monto inicial y es invocado dinámicamente cada vez que se altera el balance. La validación de `AbonarCantidad` previene que se abone un número negativo o mayor al saldo. La alerta de seguridad se emite cuando el crédito se vuelve riesgoso al capitalizar intereses.
- **Puntos Asignados:** 12.5 / 12.5 puntos.

### Manejo de Memoria (25% = 12.5 puntos posibles)
- **Criterio de Éxito:** El estudiante explica correctamente en comentarios por qué el objeto cambió de estado tras pasar por el GestorDeCobranza.
- **Análisis:** **No se incluyó ninguna explicación en comentarios** en el código (ni en `Main.java` ni en `GestorDeCobranza.java`) sobre el paso por referencia o el comportamiento de la memoria al modificar el objeto.
- **Puntos Asignados:** 0 / 12.5 puntos.

## 3. Escenario de Prueba (El main)
- **Observaciones:** La clase principal fue nombrada `Main` en lugar de `SistemaBancario` como indicaba el enunciado. La instanciación del crédito no siguió la secuencia exacta solicitada (usó el constructor 2 de 4 argumentos en lugar del constructor 1 de 3 argumentos). Además, la clase `GestorDeCobranza` tiene un defecto de diseño menor en su constructor (`this.c=c;` sin recibir parámetros). Debido a los errores de sintaxis, no fue posible ejecutar los casos de simulación de tiempo y auditoría, pero la estructura estática del código ha sido analizada.

---
## Calificación Final: 27.5 / 50 puntos
